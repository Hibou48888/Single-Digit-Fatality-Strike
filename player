//not sure if this compiles either and that's my fault

public class player
{
  int x;
  int range;
  
  static void player()
  {
    x = width/2;
    range = 300;
  }
  
  void render()
  {
    rect(x, height/2, 50, 300);
  }
}
