public class player
{
  float range;
  float playerX;
  float playerY;
  
  player()
  {
    range = 300;
    playerX = width/2;
    playerY = height/2;
  }
  
  void render()
  {
    fill(255);
    rect(playerX, playerY, 50, 300);
  }
}
